---
fixes:
  - |
    Remove all mentions of Canals by renaming some variables.
    `__canals_input__` and `__canals_ouput__` have been renamed respectively to `__deepstack_input__` and `__deepstack_ouput__`.
    `CANALS_VARIADIC_ANNOTATION` has been renamed to `DEEPSTACK_VARIADIC_ANNOTATION` and it's value changed from `__canals__variadic_t` to `__deepstack__variadic_t`.
    Default Pipeline `debug_path` has been changed from `.canals_debug` to `.deepstack_debug`.
